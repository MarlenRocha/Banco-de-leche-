json.extract! dosificacion, :id, :fecha_dosificacion, :dosificacion, :toma, :administracion, :reciennacido_id, :area_id, :etiqueta_id, :created_at, :updated_at
json.url dosificacion_url(dosificacion, format: :json)
